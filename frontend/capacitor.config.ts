import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.retofit31.app',
  appName: 'RetoFit31',
   webDir: 'dist/frontend/browser'    // 👈 debe coincidir con outputPath
};

export default config;
